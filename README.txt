SEGMENTS VERSION 2 — FULL SITE PACKAGE

CONTENTS
- index.html
- styles.css
- script.js
- works.js (54 projects)
- assets/images/ (copy your project images here)

REQUIRED IMAGE NAMING
The works.js file expects images such as:
assets/images/investigation-01.png
through the image paths listed in works.js.

TO TEST
1. Place the image files inside assets/images.
2. Open index.html in a browser, or use a local web server.
3. The page displays a visible warning wherever an image is missing.

TO DEPLOY TO GITHUB PAGES
Copy the contents of this folder into the repository root. Do not place the package inside a v2 subfolder unless you intentionally want the site at /SEGMENTS/v2/.

This package contains the completed Version 2 interface, not the earlier “v2 preview” labeling.
